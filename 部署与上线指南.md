# 标注系统上线指南（Vercel + Next.js + Clerk + MongoDB Atlas）

本文档说明本仓库 **IAA 标注问卷** 如何完成 **本地验证** 与 **公网部署**。项目已从单机版（`legacy/server.js` + `legacy/index.html`）迁移为 **Next.js App Router**，并接入 **Clerk** 与 **MongoDB（或本地文件回退）**。可按章节顺序对照操作。

**技术路线：**

| 组件 | 作用 |
|------|------|
| **Vercel** | 网站托管、HTTPS、环境变量、从 Git 自动部署 |
| **Next.js**（App Router） | 页面 + Route Handlers（`/api/*`） |
| **Clerk**（建议仅 GitHub） | 登录与身份；**Dashboard → Users** 可查看谁登录过 |
| **MongoDB Atlas**（免费 M0） | 生产环境存储每位用户的进度与答案；未配置时本地开发可写入 `data/clerk-progress.json` |

**官方英文文档入口（实施时以官网为准）：**

- Next.js：<https://nextjs.org/docs/app/getting-started>
- Clerk + Next.js Quickstart：<https://clerk.com/docs/quickstarts/nextjs>（当前仓库亦参考 [App Router 入门](https://clerk.com/docs/nextjs/getting-started/quickstart)）
- Clerk 社交登录（含 GitHub）：<https://clerk.com/docs/authentication/social-connections/overview>
- Clerk 在 API 中校验用户：<https://clerk.com/docs/references/nextjs/auth>
- Vercel 部署 Next.js：<https://vercel.com/docs/frameworks/nextjs>
- MongoDB Atlas 入门：<https://www.mongodb.com/docs/atlas/getting-started/>
- Node.js 官方驱动：<https://www.mongodb.com/docs/drivers/node/current/>

---

## 一、四样东西各干什么（直观理解）

- **Vercel**：把站点挂到公网，保管密钥（环境变量），推送代码后自动构建部署。
- **Next.js**：浏览器里的页面 + 服务端读取 `readable/`、读写数据库的 API。
- **Clerk**：未登录不能访问受保护页面与 `/api/*`；在 Clerk 后台查看用户列表做人工排查。
- **MongoDB Atlas**：云端存储；按 **Clerk 用户 ID** 区分进度，同一用户下再按 **数据集 1–20** 存答案。

**数据流简述：** 用户打开站点 →（未登录则跳转 Clerk 登录）→ 前端请求同源 `/api/*` → 服务端 `auth()` 校验 → 读 MongoDB（或本地回退文件）与 `readable/`。

---

## 二、开始前准备

1. **GitHub 账号**（托管代码；若启用 GitHub 登录，标注员也用 GitHub）。
2. **Node.js LTS**（本地开发与构建）。
3. 能在项目目录使用终端：`npm install`、`npm run dev`、`npm run build`。
4. **时间预期**：首次部署建议预留 **半天到一天**（含排错）。

---

## 三、推荐执行顺序（含当前进度）

| 顺序 | 内容 | 说明 |
|------|------|------|
| 1 | **MongoDB Atlas** | 集群、数据库用户、**Network Access** 含 `0.0.0.0/0`、保存 **`MONGODB_URI`** |
| 2 | **Clerk** | 创建 Application、启用 **GitHub**、复制 **Publishable / Secret Key** |
| 3 | **本地仓库** | **已完成**：Next.js + Clerk + API + 标注页组件已在本仓库；**你需**：配置 `.env.local` 并 `npm run dev` 自测 |
| 4 | **GitHub 仓库** | 推送代码（勿提交 `.env.local`） |
| 5 | **Vercel** | Import 项目、配置与本地一致的环境变量、部署 |
| 6 | **Clerk 生产域名** | 把 Vercel 域名加入 **Allowed origins / redirect URLs**（以 Dashboard 为准） |
| 7 | **自检** | 见下文「上线前自检清单」 |

---

## 四、第 1 步：MongoDB Atlas

**目标：** 在云端有可用的数据库，并得到 **`MONGODB_URI` 连接字符串**。

1. 注册/登录 [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)。
2. 创建 **Free M0** 集群（区域可按教程或就近选择）。
3. **Database Access**：新建数据库用户（用户名 + **强密码**），保存密码。
4. **Network Access**：添加 **`0.0.0.0/0`**（允许任意 IP，便于 Vercel Serverless 访问）。安全依赖：**强密码、URI 保密、应用层认证（Clerk）**。
5. **Connect** → **Drivers** → 复制 **`mongodb+srv://...`**，将 `<password>` 替换为数据库用户密码。

**本步完成标志：** 你有一份可用的 `MONGODB_URI`（先写入本地 `.env.local`，部署时再写入 Vercel）。

**可选数据库名：** 默认使用数据库名 **`iaa_quiz`**（集合 **`progress`**）。若需自定义，可在环境变量中设置 **`MONGODB_DB_NAME`**（与代码实现一致）。

---

## 五、第 2 步：Clerk（建议仅 GitHub 登录）

**目标：** 启用 GitHub OAuth，拿到 **Publishable Key** 与 **Secret Key**；日后在 **Dashboard → Users** 查看登录用户。

1. 注册/登录 [Clerk](https://clerk.com)，创建 **Application**。
2. 在认证设置中启用 **GitHub**（Social / OAuth 相关入口以当前 Dashboard 为准）。
3. 按 Clerk 指引在 [GitHub OAuth Apps](https://github.com/settings/developers) 创建 OAuth App：**Authorization callback URL** 以 Clerk 文档/界面提示为准。
4. 将 GitHub 的 **Client ID**、**Client Secret** 填回 Clerk。
5. 可选：关闭邮箱密码等其它登录方式，仅保留 GitHub。
6. **API Keys** 页面复制：
   - **Publishable Key**
   - **Secret Key**（仅服务端使用，勿提交到公开仓库）

**环境变量（与本仓库一致）：**

- `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`
- `CLERK_SECRET_KEY`

将上述变量写入项目根目录 **`.env.local`**（该文件已在 **`.gitignore`** 中，勿提交）。

**本步完成标志：** `.env.local` 已配置；Clerk 控制台中 GitHub 连接状态正常。

---

## 六、第 3 步：本地运行与验证（当前仓库）

**目标：** 在本机完成依赖安装、环境变量配置，确认能登录并完整标注一轮。

**说明：** 仓库已包含 Next.js 应用、Clerk、`src/middleware.ts` 鉴权、`src/app/api/*` 与 `src/components/QuizApp.tsx`，**无需**再按旧文档从零搭建项目。

1. 在项目根目录执行：

   ```bash
   npm install
   ```

2. 复制 **`.env.example`** 为 **`.env.local`**，至少填写：
   - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`
   - `CLERK_SECRET_KEY`
   - （推荐）`MONGODB_URI` — 若暂不填，本地会将进度写入 **`data/clerk-progress.json`**（按用户隔离）；**生产环境**（Vercel）务必配置 `MONGODB_URI`。

3. 在 Clerk Dashboard 中，将 **`http://localhost:3000`** 加入允许的域名 / 回调（具体名称以 Clerk 当前界面为准，区分 Development 与 Production）。

4. 启动开发服务器：

   ```bash
   npm run dev
   ```

5. 浏览器访问 **http://localhost:3000**，完成 **GitHub 登录** 后，应进入标注流程；先填写「信息统计」再答题。

6. （可选）执行 **`npm run build`**，确认能成功构建。

**常见错误：** Key 拼写错误、Clerk 未添加本地域名导致无法回调；未配置 `MONGODB_URI` 时，线上部署会缺少持久化（仅本地文件回退）。

**旧版单机入口（仅供参考）：**  
原 `server.js` / `index.html` 已移至 **`legacy/`** 目录。如需临时运行旧版：`npm run legacy`。

---

## 七、第 4 步：仓库内已实现内容（便于对照，无需重复开发）

以下已在当前代码中实现，部署时只需保证 **环境变量** 与 **MongoDB 网络访问** 正确即可。

| 项目 | 说明 |
|------|------|
| 鉴权 | `src/middleware.ts`：除 `/sign-in`、`/sign-up` 外需登录；API 路由内使用 `auth()` |
| 页面 | `src/app/page.tsx` + `src/components/QuizApp.tsx` |
| 登录页 | `src/app/sign-in/[[...sign-in]]`、`src/app/sign-up/[[...sign-up]]` |
| API | `GET/POST` `src/app/api/questions`、`progress`、`metadata`、`answer` |
| 题库 | 仅服务端读取仓库根目录 **`readable/step3_partXX_20_readable.json`**，**未**放入 `public/` |
| 进度存储 | 有 `MONGODB_URI` 时写入 MongoDB（库默认 `iaa_quiz`，集合 `progress`）；否则本地 **`data/clerk-progress.json`** |

---

## 八、第 5 步：GitHub 与 Vercel 部署（后续主线）

**目标：** 获得形如 `https://你的项目.vercel.app` 的公网地址，标注员通过 **GitHub 登录** 后使用。

1. **推送代码到 GitHub**  
   - 确认 **未提交** `.env.local`、任何密钥或连接串。  
   - 确认 **`readable/`** 已随仓库提交（否则线上无法读题）。

2. **登录 [Vercel](https://vercel.com)**  
   - **Import** 你的 GitHub 仓库，Framework 选择 **Next.js**（通常自动识别）。

3. **配置环境变量（与本地 `.env.local` 一致）**  
   在 Vercel 项目 **Settings → Environment Variables** 中至少添加（Production / Preview 按需勾选）：

   - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`
   - `CLERK_SECRET_KEY`
   - `MONGODB_URI`（**强烈建议生产环境必配**）
   - （可选）`MONGODB_DB_NAME`

4. **部署**  
   - 保存变量后 **Deploy**（或推送新 commit 触发部署）。

5. **Clerk 生产域名**  
   - 在 **Clerk Dashboard** 中，将 Vercel 提供的 **HTTPS 域名**（如 `https://xxx.vercel.app`）加入 **Allowed origins / redirect URLs**（名称以 Clerk 当前界面为准，区分 **Production**）。

6. **验证**  
   - 使用无痕窗口打开 Vercel 域名，完成 GitHub 登录 → 完整走一遍标注流程；在 **MongoDB Atlas** 中确认 `progress` 集合有数据写入（若已配置 URI）。

**常见错误：** Vercel 未配置环境变量导致线上认证或数据库失败；Clerk 未添加生产域名导致登录回调失败；Atlas 未开放 `0.0.0.0/0` 导致线上无法连库。

---

## 九、上线前自检清单

- [ ] **未登录**无法获取题目（不能通过 `public/` 或固定 URL 直接下载整套 `readable` JSON）。
- [ ] **登录后**题目加载与保存答案正常。
- [ ] **Clerk → Users** 能看到测试账号。
- [ ] **不同 GitHub 账号**登录后进度互不覆盖（MongoDB 按 `clerkUserId` 隔离）。
- [ ] 生产环境使用 **HTTPS**（Vercel 默认提供）。
- [ ] **生产环境已配置 `MONGODB_URI`**（避免仅依赖本地文件回退）。

---

## 十、安全与预期（简要）

- **已实现：** 公网访问需登录；服务端校验身份后再读写数据；密钥仅存环境变量；可从 Clerk 用户列表做人工审计。
- **已知取舍：** 任何拥有 GitHub 账号且知道链接的人都可使用；依赖链接不外传 + 定期查看 Users。
- **无法仅靠技术避免：** 已登录用户截屏、复制题目外发（内部风险）。

---

## 十一、依赖说明

本项目 **`package.json`** 已包含主要依赖（如 `next`、`react`、`@clerk/nextjs`、`mongodb`、`marked`、`isomorphic-dompurify` 等）。本地执行 **`npm install`** 即可。

若曾使用旧版单机 **`data/progress.json`**，需要迁入云端时，可按 MongoDB 文档格式手动导入或编写一次性脚本（非部署必需）。

---

## 十二、文件对应关系（便于你对照）

| 原单机文件 | 当前仓库对应 |
|------------|----------------|
| `legacy/server.js` | `src/app/api/**/route.ts` + `src/middleware.ts` |
| `legacy/index.html` | `src/app/page.tsx`、`src/components/QuizApp.tsx` |
| `data/progress.json`（旧） | MongoDB 集合 **`progress`**（或本地 **`data/clerk-progress.json`** 多用户回退） |
| `readable/*.json` | 仍位于仓库根目录 **`readable/`**，仅服务端读取，**不放入 `public/`** |

---

*文档版本：与当前仓库 Next.js 实现一致；若官方 Dashboard 文案变更，以英文官方文档为准。*
